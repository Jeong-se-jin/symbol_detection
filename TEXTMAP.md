# textmap.py — 글자를 심볼·배관에 붙인다

`pipeline.py` 는 글자를 **지우는 대상**으로만 다룬다 (4단계 `textink.py`). 이 모듈은 같은
글자를 **붙이는 대상**으로 다시 읽는다. 입력은 `pipeline.py` 가 이미 낸 세 파일이고,
그 파일들을 제자리에서 고친다.

```
python pipeline.py input/ML103480545_page65.pdf out/full     # 먼저 이것
python textmap.py out/full                                   # 그 다음 이것
python textmap_render.py out/full                            # 그림
```

## 파이프라인 안에서의 자리

```
pipeline.py  1 텍스트 → 2 타원 태그 제거 → 3 심볼 → 4 선
                                                    │
                                                    ↓  texts.json · symbols.json · lines.json
textmap.py   0 오독 제거 → 2 심볼 내부 → 3 커넥터 → 3.5 밸브 → 4 라인번호 → 5 리더
                                                    │
                                                    ↓  같은 세 파일 + text_map.json
textmap_render.py                                   01 · 09 · 10 png
```

`pipeline.py` 에는 아직 안 붙였다. 따로 돌린다.

---

## 순서가 곧 우선순위

앞 규칙이 가져간 텍스트는 뒤 규칙의 후보에서 빠진다. 순서를 바꾸면 결과가 바뀌고,
실제로 **3.5 를 4·5 앞에 두는 것이 5 의 오검출을 없앴다** (아래 참조).

| | 규칙 | 무엇을 보는가 | page_001 |
|---|---|---|---|
| **0** | 심볼 오독 제거 | bbox 가 carrier 심볼과 `IoU >= 0.5` | **12** 제거 |
| **2** | 심볼 내부 | 기하 도형 심볼 bbox 에 **완전히** 들어감 | 303 |
| **3** | 커넥터 이웃 | 커넥터 장축 양옆 `<= 10px` | 59 |
| **3.5** | 밸브 태그 | `^V\s?\d` → **가장 가까운 심볼** | 95 |
| **4** | 라인 번호 | `L###` 로 끝남 → `<= 10px` 나란한 run | 69 |
| **5** | 리더 추적 | 리더 선을 **잉크로 따라가** run 또는 심볼 | 108 |

```
texts 887 → 매핑 634 · 오독 제거 12 · 미매핑 241
글자가 붙은 심볼 229 / 284      글자가 붙은 run 139 / 1031
```

규칙 1 은 없다. "여러 개면 쉼표로 잇는다" 는 출력 규칙이라 번호를 쓰지 않았다.

---

## 0 — 심볼을 글자로 읽은 것 걷어내기

OCR 이 밸브 그림을 글자로 읽는다. bbox 가 심볼과 거의 그대로 겹치면 그것이다.

```
T0575 'D'     ↔ C0130 IoU 0.855      T0438 '['    ↔ C0104 0.801
T0008 '10'    ↔ C0047 0.791          T0149 '|0'   ↔ C0105 0.790
T0620 'ema'   ↔ C0138 0.773          T0242 'H'    ↔ C0102 0.751
T0316 'A'     ↔ C0066 0.748          T0246 '-'    ↔ C0085 0.743
T0009 'N'     ↔ C0019 0.735          T0437 'e'    ↔ C0128 0.734
T0244 '[T1'   ↔ C0100 0.694          T0011 '-RM'  ↔ C0058 0.690
```

**carrier 에만 건다.** 도형 심볼(boxed·frame)은 글자를 담는 그릇이라 태그 안
`PMS OP CMT` 가 bbox 를 0.72 까지 채운다 — 그것은 오독이 아니라 읽어야 할 글자다.
carrier 쪽은 0.86~0.69 로 12개이고 그 다음 값이 0.37 이라 사이가 비어 있다.

---

## 2 — 심볼 내부

bbox 에 **완전히** 들어간 것만. 걸치는 것은 제외한다. 심볼이 겹쳐 있으면
**가장 작은** 것에 붙인다 — 큰 상자에 붙이면 바깥 상자가 안쪽 코드까지 다 먹는다.

carrier 는 제외한다. "배관이 끊긴 구간" 이라 글자를 담는 도형이 없다.

```
frame 98/98 · connector 31/31 · boxed 11/12 · ring 1/1
F0001 "LT, 021"    B0003 "MT 02B, CORE, MAKEUP, TANK B"    R0001 "REACTOR, VESSEL"
```

## 3 — 커넥터 이웃

오프페이지 커넥터는 장축 양옆에 두 줄(위=스트림 번호, 아래=라인 스펙)을 단다.
어느 쪽이 양옆인가는 `geometry.heading` 이 말해 준다 — 세로 커넥터(`UP`)는 좌우다.

실측 간격이 위 3~8px · 아래 0~9px 이고 남의 라인 번호가 12px 이라 **10 에서 끊는다.**
12 로 잡았을 때 N0002 가 위 12px 의 `3" EBC L034A` 를 함께 집어갔다 — 그것은 옆 배관의
라인 번호이고 N0002 의 짝은 8px 의 `3108` 이다.

```
N0002 BIDIRECTIONAL  "3108, PXS TEST HDR, APP-PXS-M6-003, E-7, 1\" EBC"
                      └ 위   └────── 내부 3개 (규칙 2) ──────┘  └ 아래
```

## 3.5 — 밸브 태그 (급한 대로)

`^V\s?\d` — V 다음에 숫자가 와야 `VESSEL` 과 홑 `V` 가 갈린다. **가장 가까운 심볼에
그냥 붙인다.** 밸브를 가려내는 판정이 아직 없어서다.

```
95개  붙은 심볼: carrier 90 · connector 4 · boxed 1
거리  0.0 이 52개 · 20px 미만 36개 · 80px 이상 6개
```

거리가 먼 6개(`V025A` 150px → N0011 등)는 근처에 밸브 심볼이 없어 엉뚱한 데 붙었다.
밸브 판정이 생기면 정리될 자리다.

**이 규칙이 4·5 보다 앞서야 한다.** 밸브 태그 옆 배관이 자유 끝으로 끝나면 규칙 5 가
그 배관을 통째로 리더로 읽는다 — 3.5 가 없을 때 `V081A` 388px · `V080A` 380px ·
`V014A/B` 222px 짜리 가짜 리더가 나왔다. 3.5 를 앞에 두자 리더 추적의 `기타` 가
54 → 32 로 줄고 **라인 번호 44개와 NOTE 32개는 하나도 안 잃었다.**

## 4 — 라인 번호 → 나란한 run

`\bL(\d{3,4})[A-Z]?\s*$`. **1차를 전부 돌고 나서** 남은 것만 2차를 본다:

```
가로 글자   1차 아래  → 2차 위        page_001  65 + 4
세로 글자   1차 오른쪽 → 2차 왼쪽
```

순서를 안 지키면 아래에 제 짝이 있는 글자가 위쪽 남의 run 을 먼저 집어간다.
run 은 **아직 글자가 안 붙은 것**만 후보이고, 여러 개면 가까운 것 → 많이 겹치는 것
순으로 하나만 고른다.

**텍스트 방향은 `rot` 이 아니라 bbox 장단축으로 잰다.** `rot` 은 그 글자를 잡아낸 OCR
패스(0도/90도)이지 글자가 누운 방향이 아니다 — 라인 번호 113개 중 **40개가 어긋난다**
(`1" BBB L011A` 는 `rot=90도` 인데 bbox 가 143×31 가로다).

문턱은 5 → 8 → 10 으로 넓혔다. 후보 run 이 하나뿐인 세로 라벨이 문턱 바로 밖에서
떨어져 나갔다 (`8" BTA L025B` 8px · `8" BTA L007A` 10px).

## 5 — 리더 추적

앞 네 규칙이 **근접**으로 붙였다면 이쪽은 **실제로 이어진 잉크**를 밟는다.
길이로 리더를 가르지 않는다. 리더는 몇 번을 꺾든 리더이고, 배관을 만나는 자리는
거기서 길이 갈라진다 — `leader.py` 가 골격 교차수로 하던 판정("꺾임은 통과하고
분기만 잡는다")을 run 그래프 위에서 그대로 한다.

```
출발  글자 쪽 끝이 **아무것도 안 달린 자유 끝**인 run  (bbox 에서 <= 15px)
진행  반대쪽 끝에 이어지는 run 이 하나뿐이면 방향이 바뀌어도 계속 간다
정지  (a) 둘 이상이 모이는 분기   (b) 채택 심볼에 닿음   (c) 이미 글자가 붙은 run
```

**자유 끝 조건이 핵심이다.** 리더는 라벨에서 홀로 출발한다. 그 끝에 이미 뭔가
이어져 있으면 분기 근처를 지나가는 남의 배관이고, 옆에 놓인 글자를 붙이면 매핑이
엉뚱해진다. 이 조건을 넣자 조각 3개 이상이 전멸하고 리더 최대 길이가 534 → 190px 로
내려왔다.

```
108건   run 79 · 심볼 29      라인번호 44 · NOTE 32 · 기타 32
조각    1개 29 · 2개 75 · 3개 4
```

`tol 15` / `snap 10` 은 5/6 에서 넓힌 값이다. 대각선 run 은 끝점이 격자에 안 떨어져
엣지의 `at` 이 7~8px 어긋나고(`8" BTA L016A`), 라벨과 리더 사이가 13~14px 벌어진 것이
있다(`1" EBC L053A`). 20 까지 올리면 라인 번호는 안 늘고 잡동사니만 는다 (44 → 44,
기타 54 → 99).

---

## 플래그

```
python textmap.py <out_dir> [옵션]
```

| 플래그 | 기본 | 무엇 |
|---|---|---|
| `--conn-gap` | **10** | 커넥터 이웃으로 볼 최대 간격(px) |
| `--line-gap` | **10** | 라인 번호와 나란한 run 사이 최대 간격(px) |
| `--leader-tol` | **15** | 텍스트 bbox ↔ 리더 자유 끝점(px) |
| `--leader-snap` | **10** | 엣지 접점이 run 의 그 끝에 속한다고 볼 거리(px) |
| `--dry-run` | — | 파일을 고치지 않고 수치만 낸다 |

플래그가 없는 상수는 `textmap.py` 머리에 있다:

```
LINE_NO        r'\bL(\d{3,4})[A-Z]?\s*$'    라인 번호 꼴
VALVE_TAG      r'^V\s?\d'                   밸브 태그 꼴
MISREAD_IOU    0.5                          심볼 오독 문턱
MISREAD_SOURCES ('carrier',)                오독을 찾을 심볼 갈래
GEOM_SOURCES   ('frame','ring','connector','boxed')   규칙 2 대상
LEADER_HOPS    12                           리더 최대 홉
CONN_SIDES     heading → (1차, 2차) 방향
```

```
python textmap_render.py <out_dir> [texts|map|ids|all]
```

---

## 산출물

```
texts.json      texts[] 에 id · dropped · link · link_rule · link_side · link_gap
                · link_at · link_leader_runs 추가. 맨 아래 text_map {params, stats}
symbols.json    geometry.tag_text (쉼표로 이음) · geometry.tag_text_ids · text_links[]
lines.json      runs[] 에 text · text_ids · text_links[]
text_map.json   params · stats · links[] 634건 · dropped[] 12건 · symbols{} · runs{}
```

한 링크:

```json
{"text": "T0003", "raw": "1\" JBD L033A", "bbox": [1586, 35, 1727, 63],
 "rule": "leader", "target_kind": "run", "target": 5438,
 "side": "leader", "gap": 129.0, "at": [1488.0, 136.5],
 "leader_runs": [1, 3590], "segments": 2}
```

`at` 은 `symbol_links` 의 `at` 과 같은 뜻이다 — **리더만** 엣지에서 가져온 실제 접점이고,
나머지 규칙의 `at` 은 "가장 가까운 점" 을 계산한 값이다. 그래서 `10_text_map.png` 는
**리더의 접점만 찍는다** — 나머지를 찍으면 근거가 있는 것처럼 보인다.

리더 링크의 `gap` 은 거리가 아니라 **리더 총길이**다.

**재실행해도 같은 결과가 나온다.** `apply_to` 가 지난 매핑(`tag_text` · `text` ·
`text_links`)을 먼저 지운다 — 안 지우면 이번에 짝을 잃은 심볼이 옛 값을 그대로 달고
있어 문턱을 좁힌 일이 결과에 안 보인다.

**텍스트 id 는 좌표순이다** (`T0001` 이 좌상단). OCR 출력 순서는 타일 순서라 재실행에
흔들린다.

---

## 그림

| 파일 | 무엇 |
|---|---|
| `01_texts.png` | 회색 raw · 초록 남긴 것 · **빨간 ✕ 심볼 오독 12개** + 짝 심볼 bbox(마젠타) |
| `09_component_id.png` | 심볼 id · run id 지도. 심볼 284/284 · run 943/1031 표기 |
| `10_text_map.png` | 규칙별 색. 연두 leader(밟은 조각 굵게 + 접점) · 마젠타 valve_tag · 초록 inside · 주황 connector · 파랑 line_no |

`09` 는 라벨 자리를 16px 격자로 예약하고 심볼을 run 보다 **먼저** 찍는다 — 심볼이
284개뿐인데 run 1031개에 자리를 빼앗기면 id 없는 도형이 남는다.

---

## 남은 것

- **미매핑 241개.** NOTE 문단 · 범례 · 표제란처럼 붙을 데가 없는 글자가 대부분이다.
- **밸브 판정이 없다.** 3.5 가 "가장 가까운 심볼" 로 때우고 있다.
- **리더 `기타` 32개** 중 일부는 여전히 리더가 아니다. 조각 1개짜리(29개)는 "글자 옆에서
  끝나는 선 하나" 라 리더인지 배관 끝인지 구분이 안 된다.
- `pipeline.py` 에 5단계로 편입하지 않았다.
- 같은 라인 번호가 여러 run 에 붙으면 그것들이 **그래프상 같은 연결 성분**에 있어야
  한다. page_001 에서 4쌍 전부 일치했다 — 거리 문턱 대신 이 조건을 검증에 쓸 수 있다.
