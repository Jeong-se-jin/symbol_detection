# pipeline.py — 심볼 검출에서 선 검출까지

```
python pipeline.py <img|pdf> <out_dir>
```

`detect.py` 가 심볼만 낸다면 이쪽은 **배관 그래프까지** 간다. 네 단계이고, 순서가 곧
설계다 — 각 단계는 다음 단계의 입력을 치우려고 존재한다.

```
1 텍스트   PaddleOCR 로 글자를 읽는다 (ocr_paddle.py, 다른 인터프리터)
2 타원     라인 클래스 경계 태그와 거기 매달린 리더를 지운다
3 심볼     지운 도면에서 심볼을 검출한다 (detect.py 그대로)
4 선       글자와 심볼을 지운 뒤 배관 run 을 뽑는다
```

## 두 스케일

| | 무엇 | page_001 | 무엇에 쓰나 |
|---|---|---|---|
| `ref_h` | OCR 글자 높이 중앙값 | 29px | 텍스트 관련 임계 |
| `draw_h` | 도면 자체의 스케일 | 19.6px | 기하 임계 |

`draw_h` 는 **계기 버블 지름 × 0.215** 로 잰다 — 버블은 어느 P&ID 에서나 규격 크기라
배율을 그대로 말해 준다. 버블이 5개 미만이면 `stroke × 5.6` 으로 물러선다.

둘을 분리한 이유는 **OCR 상자가 글자의 고정 배수가 아니기** 때문이다. 같은 도면이라도
1bit 스캔은 상자가 19px, 230dpi 렌더는 14px 로 나온다. `ref_h` 로 기하를 몰면 임계가
1/3씩 부풀어 화살촉 검출이 거의 다 실패한다.

---

## 1단계 — 텍스트

`ocr_paddle.py`. **이 파일만 다른 인터프리터로 돈다** (paddle 은
`/home/rx/project/STA-main/.venv-detect` 에 있다). pipeline.py 가 페이지를 한 번 렌더해
`00_page.png` 로 써 두고 그것을 넘긴다 — PDF 를 여는 v34 가 skimage 를 물고 있어
paddle 쪽 venv 에서는 못 쓰기 때문이다.

- **타일** 1280px, 겹침 384px → 35장. 전지를 통째로 넣으면 검출 해상도에 맞춰 축소되어
  10px 짜리 계기 태그가 뭉개진다.
- **90도 회전 패스**를 한 번 더 돈다. 세로로 누운 라인 번호(`1" BBB L071A`)를
  `use_textline_orientation` 만으로는 못 편다. page_001 에서 세로 상자 62개 중 **54개가
  이 패스에서** 나왔다 (0도 패스는 8개).
- 겹침이 만든 중복을 IoU·포함률로 묶고 `점수 × 글자수` 가 가장 큰 것만 남긴다.
  점수만 보면 한 글자 조각이 온전한 태그를 이긴다.
- **중복 제거 전 목록(`texts_raw`)도 남긴다.** 지우개 마스크는 그쪽을 쓴다 — 한 글줄의
  왼쪽 절반과 오른쪽 절반이 서로 다른 타일에서 잡히면 둘이 겹쳐 '같은 영역' 으로 묶이고
  한쪽이 버려진다. NOTES 문단이 그래서 절반만 지워졌다.

**캐시** (`ocrcache.py`) — 렌더된 페이지의 **내용 해시** + OCR 설정이 키다. 출력
디렉터리로 키를 삼으면 같은 폴더에 다른 도면을 돌렸을 때 남의 글자를 조용히 물려받는다.
적중하면 paddle 프로세스를 아예 안 띄운다 (13분 → 0초).

page_001: `texts 887 · texts_raw 3,278 · ref_h 29`

> 검출 문턱을 푸는 옵션(`--ocr-det-box-thresh` 등)이 있지만 **쓰지 않는다.** 한 글자짜리를
> 잡으려고 풀었더니 상자가 커져 `ref_h` 가 29 → 36 으로 뛰고, 넓어진 상자가 배관을 먹어
> 엣지를 218개 잃었다.

---

## 2단계 — 타원 경계 태그와 리더 제거

`corner.py` · `leader.py`.

`boxed_tags` 는 닫힌 윤곽 · 내부 잉크 · 장단비만 잰다. 끝이 둥근지 각진지는 측정 항목에
없어서 성격이 다른 두 갈래가 같은 `elongated_box` 로 나온다:

| 끝단 | page_001 | 무엇인가 |
|---|---|---|
| 둥근 (STADIUM) | 18 | `ECE│ECB` — 배관 위 라인 클래스 경계. 그래프에서 **분할점** |
| 각진 (RECT) | 10 | `PMS│OP│CMT` — 점선 신호선으로 밸브에 묶임. **제어 태그** |

**판별**: 장축 단면에서 끝단이 평지(90%)를 깨는 길이 / 짧은 변. 반지름 `r` 반원 캡은
`d = .282 × short`, 각진 벽은 0. 실측은 각진 10개가 전부 **0.000**, 둥근 20개가
**0.258~0.320** 으로 사이가 비어 있다.

**용기는 칸막이로 가른다** — 캡슐형 탱크도 끝이 둥글다. 태그는 코드를 칸에 나눠 담고
용기는 속이 비어 있으므로 `cells >= 2` 를 함께 건다. 크기 임계는 배율을 따라가야 하지만
칸막이는 어느 배율에서도 칸막이다.

**리더**는 칸막이 한가운데에서 나온 1px 선 하나다. 골격을 뜨고 한 픽셀씩 걸으며 8-이웃
고리의 **교차수**를 센다 — 끝점 1 · 지나가는 자리 2 · 분기 3 이상. 분기 픽셀은 밟지 않고
멈춘다. 교차수는 90도 꺾임도 2 로 내므로 "꺾임은 통과하고 분기만 잡는다" 가 한 수로
성립한다.

page_001: 태그 18개, 리더 18개(전부 `JUNCTION` 에서 정지, 길이 58~218px)

---

## 3단계 — 심볼

`detect.py` 를 그대로 부른다. 채택 230 (frame 98 · carrier 88 · connector 31 · boxed 12 ·
ring 1) · 기각 54.

**기각분도 4단계의 지우기 대상에 넣는다.** 기각은 "심볼이 아니다" 가 아니라 "이 임계를
못 넘었다" 는 뜻이고, page_001 의 기각 54개는 하나도 채택 심볼과 겹치지 않는다 — 전부
설명되지 않은 잉크이며, 지우지 않으면 배관 run 으로 샌다.

> **글자를 먼저 지우고 심볼을 찾는 순서는 안 된다.** 옮겨 온 쪽(RF-DETR)은 그렇게 하지만
> 우리 기하 검출기는 **내부 잉크를 게이트로 쓴다.** 버블 안 코드를 먼저 지우면 빈 원이
> 되어 탈락한다 — 실제로 frame 98 → 0 이 되었다. `--symbols-after-text` 로 재볼 수 있게만
> 남겨 두었다.

---

## 4단계 — 선

### (a) 글자 지우기 — `textink.py`

OCR 상자를 흰색으로 칠하지 않는다. 라벨은 배관 **위에** 앉으므로 상자를 칠하면 그 아래
배관이 같이 사라진다.

```
심볼이 **실제로 차지한 자리**를 칠한다 (bbox 가 아니라 윤곽을 다시 떠서)
OCR 상자마다:
  상자 + 여백(짧은 변 × 0.45) 으로 창을 잡는다
  창 안 잉크만 잘라 연결 성분을 **다시** 만든다
    창 테두리에 닿음                        → 남김 (밖으로 이어진다 = 배관)
    심볼 자리와 많이 겹치고 그 밖으로도 뻗음 → 남김 (심볼 몸통)
    그 외                                   → 지움
```

**왜 성분 단위를 버렸는가.** 원래는 잉크 전체를 연결 성분으로 나눠 "이 성분이 글자인가"
를 물었다 (80% 가 상자 안에 있고 상자보다 크게 삐져나가지 않을 것). 그 방식은 배관에
**닿은** 글자를 원리적으로 다룰 수 없다 — 글자 한 자가 수만 픽셀짜리 배관망의 일부가
되어 어떤 문턱으로도 안 갈린다. 상자 안에서 잘라 놓고 보면 그 글자는 독립 덩어리가
되고, 판정이 하나로 줄어든다.

**여백이 필요한 이유**: OCR 상자는 글자에 딱 맞으므로, 여백이 없으면 글자 자신이
테두리에 닿아 배관으로 오인된다 (그렇게 했더니 지운 잉크가 207k → 371k 로 줄고 run 이
4배가 되었다).

**"마주보는 두 변을 잇는가"(관통) 로 판정하다 바꿨다.** 그 조건은 창 안에서 끝나는 배관
조각을 전부 글자로 판정해 tee 가 4개까지 무너졌다. 한쪽으로만 나가도 창 밖으로 이어지는
것은 마찬가지다.

**심볼 자리를 bbox 가 아니라 윤곽으로 잡는 이유**: 오프페이지 커넥터는 왼쪽이 뾰족한
오각형이라 bbox 안에 빈 삼각형이 생긴다. 거기 놓인 글자 `A` 가 '심볼 몸통' 으로 판정되어
살아남았다. `carrier` 는 배관이 끊긴 구간이라 도형이 없으므로 bbox 를 쓴다.

page_001: 잉크 957,394 → 206,414

### (b) 심볼 지우기

**bbox 를 배관 굵기가 될 때까지 바깥으로 넓혀** 지운다. 검출기의 bbox 가 심볼을 딱 맞게
감싸지 못하는 일이 있다 — V045 는 밸브의 위·아래 플랜지 바(폭 30px, 3줄)가 bbox 밖으로
삐져나와, bbox 만 채우면 그 바가 남고 다음 단계가 그것을 **배관으로 읽는다**. 몇 px 이
필요한지는 심볼마다 다르므로 고정 여백 대신 **무엇이 남아 있는지**를 본다: 경계 바깥의
잉크가 배관 한 가닥(2.5 × stroke)보다 넓으면 아직 심볼 몸통이다.

**형태대로 지운다.** 사각형으로 지우면 모서리의 남의 잉크가 같이 날아간다.

| shape | 지우는 모양 |
|---|---|
| `closed_circle` · `dashed_circle` | bbox 내접 타원 (+ 획 두께) |
| `elongated_box` + STADIUM | 양끝 둥근 캡슐 |
| `taper_flag` | 잉크에서 윤곽을 다시 떠서 채움 |
| 그 외 | 사각형 |

도형을 `stroke/2 + 1` 만큼 키운다 — 딱 내접시키면 윤곽선의 **바깥쪽 획**이 남는다
(버블 하나당 70px 씩, 전체 3,089px).

속에 다른 심볼이 있거나 글자를 지우고도 잉크가 3% 넘게 남으면 테두리만 지운다.
크기(`big_shape`)로 가르지 않는다 — 그 규칙은 오프페이지 커넥터(331×61) 31개를 전부
장비로 잘못 보았다. **길다는 것과 속을 품었다는 것은 다른 말이다.**

page_001: 84,054 → **`05_pipe_ink.png`**

### (c) 1패스 — 방향장 (`lines.py`, 이식분)

픽셀마다 얇은 선 커널 36방향을 씌워 잉크가 가장 많이 실리는 방향을 고르고, 같은 답을 낸
이웃끼리 묶어 run 으로 본다. 세선화를 안 하므로 대각선이 계단으로 안 부서지고, **답이
바뀌는 자리가 곧 접합점**이라 접합점 검출기를 두지 않는다.

- `min_support 0.45` 미달은 '방향 불명' 으로 버림
- `bridge_gap 12px` 로 교차가 훔쳐간 픽셀 너머의 run 을 이어붙임
- **길이 하한 없음** (`pipe_min 0`). 짧다는 것은 부스러기라는 뜻이 아니다 — 밸브 사이
  배관 토막이 없으면 그래프가 끊긴다

> 방향 수는 **36** 이다. 18(10도)은 얕은 대각선이 두 버킷 사이에 떨어져 방향 불명으로
> 버려지고, 72(2.5도)는 같은 직선이 버킷을 넘나들어 잘게 쪼개진다 (이어붙임 342 → 827,
> 엣지 694 → 603).

> `bridge_gap` 을 18px 로 키우면 대각선 점선 조각이 이어지지만, **심볼을 건너뛴 실선이
> 점선으로 판정되고**(+16) 심볼-배관 링크를 16개 잃는다. 기본 12px 을 쓴다.

### (d) 2패스 — 잔여 회수 (`residual.py`)

1패스가 못 덮은 잉크만 골격으로 떠서 경로화한다. 골격 걷기는 자를 쓰지 않으므로 프로브
(25px)보다 짧은 조각도 살아난다.

- 곧거나(`straightness ≥ 0.85`) **한 자리에서만 꺾였으면**(`최대꺾임/총꺾임 ≥ 0.5`) 선으로
  본다. 배관 위 작은 원의 호는 여러 자리에서 조금씩 돌아 빠진다
- **44개는 기존 run 의 꼬리로 흡수**한다 — 화살촉은 리더의 **일부**지 별개 선이 아니다.
  끝점(a/b)은 옮기지 않는다: 직선 run 의 끝을 꺾인 조각 끝으로 당기면 선 전체가 기울어
  원래 잉크를 벗어난다 (미설명 잉크 +630px)

page_001: 잔여 1,597px → 폴리라인 76개 (호 9 제외) → 흡수 44 · 독립 32

### (e) 접촉 — 순서가 곧 우선순위

```
1 픽셀이 실제로 맞닿음      874
2 끝-대-끝 스냅 (16px)      609
3 끝-대-중간 스냅 (16px)     28   ← 우리가 더한 것
```

3번이 없으면 내 끝이 남의 중간에 2~3px 떠서 닿는 T 접합을 1·2 어디에서도 못 잡는다.
이식분이 중간끼리의 근접을 잇지 않는 것은 옳다 — 배관 위를 **지나가기만** 하는 리더를
분기로 오인하면 도면이 자기 자신에 얽힌다. 하지만 한쪽이 여기서 **끝난다**는 것 자체가
T 접합의 증거다.

**crossover 는 버리지 않고** 남기며, 그 자리에서 run 도 자른다.

접촉을 **무엇으로 잡았는가(via)** 를 엣지에 기록한다 — 자홍(끝-대-중간)이 몰려 있으면
그 자리의 잉크가 끊겨 있다는 뜻이고, 그것은 앞 단계의 문제다.

### (f) 버리기 — 길이가 아니라 닿았는가

| 규칙 | 조건 | page_001 |
|---|---|---|
| 부스러기 | 엣지·심볼·글자 어디에도 안 닿음 | 몇 개 |
| **심볼 둘레 잔해** | 양 끝이 같은 심볼 둘레에 갇힘 **+ 엣지 0** | **44개** |
| 글자 상자 안 | run 이 상자 안에 통째로 들어감 + 접촉 없음 | 3개 |

심볼 둘레 잔해는 **두 패스를 합치고 접촉을 다 센 뒤 한 번만** 건다. 1패스 직후에 걸면
버린 잉크가 잔여로 내려와 2패스가 도로 주워 오고, 2패스에도 같은 잣대를 걸면 정상적인
경계 스텁까지 걸려 폴리라인이 88 → 18 로 무너진다.

'양 끝이 갇힘' 만 보면 안 된다 — 스텁은 13px 남짓이라 양 끝이 다 `touch`(20px) 안에
들어온다. **이어진 곳이 없다**가 함께 있어야 잔해다.

### (g) 점선 (`dashes.py`)

간격이 있느냐가 아니라 **일정하냐**를 본다. 심볼을 지우면서 뚫린 실선도 간격이 생긴다.
page_001 의 리듬은 획 20px / 간격 8px / 주기 28px.

**`sample()` 만 우리 쪽에서 교체했다.** 원래는 축 위를 한 칸씩 걸으며 읽는데, 대각선에서는
이산화된 선과 경로가 어긋나 잉크를 놓친다. 세 가지로 고쳤다:

1. 축을 걷는 대신 **선 둘레 ±band 의 잉크를 축에 투영**
2. 픽셀 하나가 덮는 축 구간(`1/max(|ux|,|uy|)`)만큼 칠함 — 45도면 픽셀이 √2 간격이라
   눈금의 29% 가 빈다
3. **1px 구멍은 메움** — `gap_min`(2.7px)보다 짧으니 정의상 간격이 아니다

```
before  I3·_1·I1·_1·I4·_1·I5·_1·I3·_9·…  gaps CV 1.25  → solid
after   I20·_8·I20·_7·I17                gaps CV 0.07  → dashed
```

**간격 하나짜리**도 잡는다(`min_gaps 1`). 조각이 `[잉크·간격·잉크]` 셋뿐이면 첫·끝을 버린
뒤 잉크가 하나도 안 남아 영영 판정되지 않는다. 그래서 잘린 양끝을 되살리되 **서로 2배
이내**일 때만 — 한쪽만 1px 인 것은 점선이 아니라 실선 끝의 잉크 파편이다.

---

## 산출물

```
00_page.png           렌더된 원본 (OCR 입력이자 캐시 키)
01_texts.png          OCR 상자 — 회색 raw(마스크) · 초록 deduped(+ 읽은 글자)
02_tags_erased.png    타원 태그·리더 제거
03_symbols.png        심볼 오버레이
04_text_erased.png    글자만 제거
05_pipe_ink.png       ← 선 검출 직전
06_lines.png          선 검출 결과
08_symbol_shapes.png  실제 검출 윤곽(초록) vs bbox(회색)
lines.json            runs · edges · symbol_links · erased_tags
symbols.json / .csv   심볼
texts.json            texts · texts_raw
metrics.json          이번 실행 수치 (직전 실행과 자동 비교)
```

`lines.json` 의 run:

```json
{"id": 1476, "length": 1906.0, "angle": 90.0, "orient": "v",
 "a": [3811.0, 2492.0], "b": [3811.0, 4398.0], "line_type": "solid",
 "tail": [[2129,4337], ...],        // 흡수한 조각 (있을 때만)
 "points": [[x,y], ...]}            // 2패스 폴리라인 (있을 때만)
```

엣지: `{"a": 1747, "b": 2872, "at": [2527,504], "kind": "tee",
       "ends": [true,false], "via": "pixels"}`

---

## page_001 (ML103480545 p65, 6600×4859)

```
텍스트   887 (raw 3,278)          타원 태그 18 + 리더
심볼     채택 230 · 기각 54
잉크     957,394 → 206,414(글자) → 84,054(심볼)
run      1,031    edge 1,511    tee 61 · crossover 1
선종     solid 954 · dashed 77
심볼-배관 273/284 개에 run 598개
설명된 잉크 98.6%                 소요 38초 (OCR 캐시 적중 시)
```

## 알려진 한계

- **글자 잔여 45개.** 대부분 OCR 이 아예 못 본 단독 문자(`D`·`H`·`V` — 주변에 아무것도
  없는 한 글자는 '글줄' 로 안 보인다)이고, **6개는 글자가 아니라 밸브 액추에이터
  박스**다 (44×45, 어느 검출기 조건에도 안 맞아 심볼로 안 잡힘)
- **`06_lines.png` 의 연회색 글자는 배경 도면**이지 잔여 잉크가 아니다. 실제 잔여는
  `05_pipe_ink.png` 에서 봐야 한다. `--line-fade 0` 으로 배경을 뺄 수 있다
- 주황색이 **2패스 선과 끝-대-끝 스냅 점에 같이** 쓰여 혼동을 준다
- 배관 위 작은 원(지름 20px, 계기 연결 노즐로 보임)은 어느 검출기에도 안 걸린다 —
  크기로도 형태로도 조건에 안 맞는다
- `probe`(25px)보다 짧은 조각은 1패스에서 구조적으로 버려진다. 2패스가 그 뒤처리를
  하지만, 2패스를 끄면 설명된 잉크가 98.6% → 97.8% 로 떨어진다 (엣지는 2개 차이)

## 주요 옵션

```
--no-residual              2패스 끄기
--absorb-radius 0          흡수 끄기
--keep-symbol-scraps       심볼 둘레 잔해 남기기
--text-margin 0.45         글자 판정 창 여백 (짧은 변 배수)
--symbol-overlap 0.35      심볼 몸통으로 볼 겹침 비율
--symbol-grow 4.0          심볼 bbox 넓히기 상한 (stroke 배수)
--no-shape-erase           심볼을 형태 대신 사각형으로 지움
--directions 36            방향장 버킷 수
--min-gaps 1               점선 판정에 필요한 간격 수
--line-width 2             06_lines.png 선 굵기
--line-fade 0.16           배경 도면 농도
--lines-legend             범례 켜기
--redo-ocr                 OCR 캐시 무시
```
